interface:
  display_name: "Jiminy"
  short_description: "Merge Gepetto-verified PRs behind exact-head gates"
  default_prompt: "Use $jiminy to execute this delivery's JIMINY_READY merge set after every gate is green on the live heads."
