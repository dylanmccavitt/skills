interface:
  display_name: "Jiminy"
  short_description: "Watch Gepetto, Pinocchio, and exact-head merge gates"
  default_prompt: "Use $jiminy to monitor the active Gepetto task and merge pull requests after every gate is green."
